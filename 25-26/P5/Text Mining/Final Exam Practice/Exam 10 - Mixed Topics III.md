# Exam 10 — Mixed Topics III
*35 questions | Correct answer marked with \**

---

**Q1.** Which of the following tokenisation decisions is considered CORRECT for the string "I'll" in most English NLP systems?
- A) Keep "I'll" as a single token
- B) Split into "I" and "'ll" to separate the clitic *
- C) Expand to "I will" as two tokens
- D) Remove it as a stop word

**Q2.** In WordNet, the relationship between "vehicle" and "car" (car is a kind of vehicle) is formally called:
- A) Meronymy
- B) Holonymy
- C) Hyponymy (car is a hyponym of vehicle) *
- D) Synonymy

**Q3.** A classifier trained on a dataset with 95% negative examples and 5% positive examples achieves 96% accuracy by always predicting "negative". Its F1 for the positive class is:
- A) 0.96
- B) 0.50
- C) 0.00 *
- D) 0.05

**Q4.** In word2vec, what is the purpose of the hidden layer weight matrix W (input → hidden)?
- A) It serves as the final word embedding matrix after training *
- B) It computes the attention scores between word pairs
- C) It stores the vocabulary's TF-IDF weights
- D) It encodes positional information for each token

**Q5.** Which of the following is an example of the "Location" semantic role?
- A) "Maria" in "Maria baked a cake"
- B) "the cake" in "Maria baked the cake"
- C) "the oven" in "Maria baked the cake in the oven" *
- D) "for her daughter" expressing the beneficiary

**Q6.** Gricean maxims in pragmatics include Quantity, Quality, Relation, and Manner. The maxim of "Quality" states that speakers should:
- A) Provide as much information as needed, but no more
- B) Be relevant to the current purpose of the exchange
- C) Not say what they believe to be false *
- D) Avoid ambiguity and be orderly

**Q7.** The NRC Emotion Lexicon maps words to eight Plutchik emotions. Which four are NOT in Ekman's six basic emotions?
- A) Joy, sadness, anger, disgust
- B) Trust, anticipation, surprise, joy
- C) Trust, anticipation, disgust, fear
- D) Trust, anticipation, surprise, disgust *

**Q8.** In the BERT fine-tuning pipeline for NER, what happens to subword tokens that are NOT the first subword of a word (e.g., "##ing" in "running")?
- A) They are assigned the same label as the first subword
- B) They are assigned a special [PAD] label
- C) Their output is discarded and only the first subword's representation is used for labelling *
- D) They receive a separate IOB label from the first subword

**Q9.** Which of the following is a characteristic of "bronze" training data for NLP?
- A) Annotated by multiple expert annotators with adjudication
- B) Automatically generated by a rule-based or model-based system without human review *
- C) Sampled from a balanced, domain-representative corpus
- D) Used exclusively for test-set evaluation

**Q10.** In the context of text classification, the "Reuters-21578" dataset is notable for:
- A) Being perfectly balanced across all topic categories
- B) Having a multi-label structure and heavy skew toward financial and commodity topics *
- C) Containing only opinion-bearing documents
- D) Being the standard benchmark for emotion classification

**Q11.** What distinguishes "polarity classification" from "subjectivity classification" in sentiment analysis?
- A) Polarity classifies documents as subjective or objective; subjectivity classifies them as positive or negative
- B) Subjectivity classification determines whether text expresses an opinion; polarity classification determines whether that opinion is positive or negative *
- C) They are identical tasks with different names
- D) Polarity classification requires aspect extraction; subjectivity does not

**Q12.** The "entity mention" in NER refers to:
- A) The canonical form of an entity in a knowledge base
- B) A specific surface-form string in text that refers to a named entity *
- C) The IOB tag assigned to the entity's head token
- D) The coreference chain identifier for the entity

**Q13.** In which scenario would macro-averaged F1 give a misleadingly optimistic view of system performance?
- A) When the dataset has equal class distribution
- B) When a rare class has very low F1 but frequent classes perform well (macro treats all classes equally) *
- C) When the dataset has only two classes
- D) When the system achieves perfect precision on all classes

**Q14.** BERT-large differs from BERT-base in which two key ways?
- A) BERT-large uses BPE tokenisation; BERT-base uses WordPiece
- B) BERT-large has 24 transformer layers and 1024 hidden dimensions vs. BERT-base's 12 layers and 768 dimensions *
- C) BERT-large was pre-trained on more languages
- D) BERT-large uses a decoder-only architecture; BERT-base uses encoder-only

**Q15.** In LDA, alpha (α) and beta (β) are Dirichlet hyperparameters. Which of the following correctly describes their effect?
- A) High α → documents are focused on fewer topics; high β → topics are focused on fewer words
- B) Low α → documents are focused on fewer topics; low β → topics are focused on fewer words *
- C) α controls the number of topics K; β controls the vocabulary size
- D) α and β only affect the initialisation of Gibbs sampling, not the final result

**Q16.** An NLP system processes tweets and achieves high precision but low recall on PERSON entities. What is the most likely explanation?
- A) The system predicts too many entities, many of which are wrong
- B) The system misses many true PERSON entities but rarely makes false predictions *
- C) The system confuses PERSON and ORGANISATION labels
- D) The system's recall is high but precision is low

**Q17.** Which of the following correctly describes what "transfer learning" achieves in the context of BERT for NLP?
- A) Translating English training data into other languages for cross-lingual evaluation
- B) Reusing weights learned from large-scale unsupervised pretraining to bootstrap performance on small labelled datasets *
- C) Transferring labels from one annotation scheme to another
- D) Moving a trained model from one hardware platform to another

**Q18.** In constituency parsing, what is a "non-terminal" node in a parse tree?
- A) A leaf node corresponding to a specific word token
- B) A phrasal category label (NP, VP, PP, S) grouping one or more words or phrases *
- C) A dependency relation arc between two words
- D) A POS tag assigned to a single token

**Q19.** Which of the following best explains why "implicit sentiment" is harder to detect than explicit sentiment?
- A) Implicit sentiment only appears in long documents
- B) Implicit sentiment requires world knowledge and event understanding, not just lexical polarity matching *
- C) Implicit sentiment is always ironic
- D) Implicit sentiment cannot be annotated by humans

**Q20.** The Hierarchical LDA (hLDA) model learns:
- A) Topic distributions that change over time
- B) A tree-structured hierarchy of topics where general topics are parents of more specific topics *
- C) Topic distributions conditioned on author identity
- D) A fixed number of topics K using a Dirichlet prior

**Q21.** According to the course, the performance of a BERT-based NER system on CoNLL 2003 English is approximately:
- A) 85 F1
- B) 88 F1
- C) 91 F1
- D) 93+ F1 *

**Q22.** Which of the following is a key insight from Ravi & Ravi (2015) about sentiment analysis research?
- A) Rule-based methods outperform machine learning for all sentiment tasks
- B) SVMs were the most widely applied ML classifier, appearing in the majority of surveyed articles *
- C) Deep learning replaced all traditional methods by 2010
- D) Aspect-based methods were never used before 2015

**Q23.** An IOB label sequence B-PER B-ORG is valid (not illegal). What does it indicate?
- A) A two-token entity spanning both tokens
- B) Two adjacent but separate named entities, each beginning a new span *
- C) An error in annotation — two B- tags can never appear consecutively
- D) A PERSON entity followed by an O (outside) token

**Q24.** In the context of language modelling, perplexity measures:
- A) The average number of topics assigned to each document
- B) How uncertain a model is when predicting the next word (lower = better model fit) *
- C) The Kappa agreement between two language models on the same data
- D) The ratio of vocabulary coverage to corpus size

**Q25.** Which statement about the distributional hypothesis and PMI is correct?
- A) PMI measures how much more often two words co-occur than would be expected if they were independent *
- B) PMI assigns higher weights to stop words because they appear everywhere
- C) PMI is equivalent to TF-IDF when corpus size is held constant
- D) PMI only works with character n-grams, not word-level co-occurrences

**Q26.** The "Instrument" semantic role is illustrated by which example?
- A) "The chef" in "The chef prepared dinner" (initiator)
- B) "dinner" in "The chef prepared dinner" (affected entity)
- C) "a sharp knife" in "The chef cut the fish with a sharp knife" *
- D) "for the guests" in "The chef prepared dinner for the guests"

**Q27.** Which property makes BPE (Byte-Pair Encoding) tokenisation robust to out-of-vocabulary words?
- A) BPE uses a character-level fallback for any unknown word
- B) BPE merges frequent character pairs iteratively, so any word can be decomposed into known subword units *
- C) BPE replaces OOV words with their nearest embedding neighbour
- D) BPE uses a special [UNK] token for words not in the vocabulary

**Q28.** In the SemEval 2014 Task 4 pipeline for ABSA, aspect polarity classification comes:
- A) Before aspect term extraction
- B) After aspect term extraction (first identify aspects, then classify their sentiment) *
- C) Simultaneously with aspect extraction in a joint model only
- D) After document-level subjectivity classification and before aspect extraction

**Q29.** Which of the following statements is FALSE about dependency parsing?
- A) Dependency parses are directed graphs with words as nodes and grammatical relations as arc labels
- B) Every word (except the root) has exactly one head
- C) Dependency parses always form a tree structure with nested phrase brackets *
- D) Dependency parses can capture long-distance grammatical relations like subject-verb agreement

**Q30.** The Bing Liu opinion lexicon contains:
- A) A list of named entity types and their associated sentiment
- B) Thousands of positive and negative opinion words compiled for sentiment analysis *
- C) Psycholinguistic categories like social processes and cognitive processes
- D) A mapping from words to Plutchik emotion categories

**Q31.** In cross-validation, stratified k-fold ensures:
- A) Each fold contains the same number of tokens, not documents
- B) Each fold preserves approximately the same class distribution as the full dataset *
- C) Different annotators create different folds
- D) The test fold is always the same across all k iterations

**Q32.** What is the role of positional encoding in the Transformer model?
- A) To encode the semantic similarity between tokens at different positions
- B) To inject information about each token's absolute or relative position, since self-attention is permutation-invariant *
- C) To mask padding tokens so the model does not attend to them
- D) To encode the sub-word type (first piece vs. continuation piece)

**Q33.** Which of the following named entity types would require an organisation gazetteer but NOT a person-name gazetteer?
- A) "Barack Obama" (PERSON)
- B) "Greenpeace" (ORGANISATION) *
- C) "Amsterdam" (LOCATION)
- D) "Monday" (TIME)

**Q34.** A text mining pipeline that extracts who said what about whom from news articles relies most heavily on which combination of NLP modules?
- A) Tokenisation and TF-IDF only
- B) NER, coreference resolution, and semantic role labelling *
- C) Topic modelling and BIO tagging only
- D) Sentence splitting and word embeddings only

**Q35.** Which of the following is the correct description of a "false negative" in NER evaluation?
- A) The system predicts an entity span that does not exist in the gold standard
- B) The system correctly identifies the entity span but assigns the wrong type
- C) A gold entity span that the system fails to predict *
- D) The system predicts the correct span boundaries but an incorrect entity type
